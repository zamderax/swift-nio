# Windows FileSystem TODO

## Notes
- Operations currently funneled through FileManager; no thread-pool reuse yet.
- Advanced features (descriptor-based handles, recursive traversal via FTS, symlink/readlink, permissions) remain TODO for future increments.
